# Docs for the Azure Web Apps Deploy action: https://github.com/azure/functions-action
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy dotnet core app to Azure Function App - durablefx1

on:
  push:
    branches:
      - durablefx
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_PACKAGE_PATH: '.' # set this to the path to your web app project, defaults to the repository root
  DOTNET_VERSION: 'v4.0' # set this to the dotnet version to use

jobs:
  build-and-deploy:
    runs-on: windows-latest
    permissions:
      id-token: write #This is required for requesting the JWT

    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@v4

      - name: Setup DotNet ${{ env.DOTNET_VERSION }} Environment
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: 'Resolve Project Dependencies Using Dotnet'
        shell: pwsh
        run: |
          pushd './${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}'
          dotnet build --configuration Release --output ./output
          popd
      
      - name: Login to Azure
        uses: azure/login@v1
        with:
<<<<<<< HEAD
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_5CE8049AE6854DA583C80E04561D68F0 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_876583C444DD4EA9BA4F742CA4E546FC }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_29FD1B35BF3D4677A1B8D24CAF74DEAC }}
                                                                   
=======
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_7F987AC2CD0447CB87AB77F79D4860A3 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_54EC8EA1DD764BA98CEC0E484B1D3D19 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_6A9A7AA0B8FE4C0EAF89DA64C5EF5F95 }}

>>>>>>> d27e749b906e21d192585f108ff99ca50e961f7d
      - name: 'Run Azure Functions Action'
        uses: Azure/functions-action@v1
        id: fa
        with:
          app-name: 'durablefx1'
          slot-name: 'Production'
          package: '${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}/output'
          